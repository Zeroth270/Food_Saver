spring:
  datasource:
    url: jdbc:mysql://switchyard.proxy.rlwy.net:38106/railway
    username: root
    password: yTjnvaYWMwPSnNTpmvyLBAprZQEEJDaG

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: GOOGLE_CLIENT_ID
            client-secret: GOOGLE_CLIENT_SECRET

  mail:
    host: smtp-relay.brevo.com
    port: 587
    username: ${BRAVO_MAIL_USERNAME}
    password: ${BRAVO_MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000