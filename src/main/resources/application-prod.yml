spring:
  datasource:
    url: jdbc:mysql://switchyard.proxy.rlwy.net:38106/railway
    username: root
    password: yTjnvaYWMwPSnNTpmvyLBAprZQEEJDaG

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: GOOGLE_CLIENT_ID
            client-secret: GOOGLE_CLIENT_SECRET

  mail:
    host: smtp.gmail.com
    port: 465
    username: MAIL_USERNAME
    password: MAIL_PASSWORD
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
